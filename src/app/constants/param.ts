export enum API_PARAM {
  status = 'status',
  minDate = 'min_date',
  maxDate = 'max_date',
  skip = 'skip',
  limit = 'limit',
  id = 'id',
  namePhone = 'name_or_phone_number',
}

export enum ROUTE_PARAM {
  shopID = 'shopID',
}
